spring:
  application:
    name: duma
  datasource:
    url: jdbc:postgresql://db:5432/duma_db
    username: duma
    password: 163duma
    driver-class-name: org.postgresql.Driver
  mongodb:
      uri: mongodb://duma:163duma@mongodb:27017/duma_db?authSource=admin
  data:
    redis:
      host: redis
      port: 6379
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: duma
    password: 163duma
  jpa:
    hibernate:
      ddl-auto: update  # cria/atualiza tabelas automaticamente
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always